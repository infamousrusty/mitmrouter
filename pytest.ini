[pytest]
minversion = 7.0
testpaths = tests
addopts = -ra -q --strict-markers
markers =
    light: fast unit tests with no I/O (always run in CI)
    medium: tests with file I/O or mocked subprocesses
    heavy: tests requiring external tools installed (skipped by default in CI)
